# Legacy code

This directory preserves earlier research scripts for provenance. The files retain their original organization and assumptions, except that comments were removed and one malformed f-string was repaired so the snapshot passes syntax compilation.

Legacy scripts may contain machine-specific paths and superseded data handling. They are not used by the reproducible commands in the repository-level README. Use the main source directories for the 73.25%/84.91% rerun.
